<!-- {{#if currentUser}}
  <h4>welcome {{currentUser.email}}</h4>
{{/if}}

{{#each users}}
  <ul>
    <li>
    <li>{{this.id}}</li>
    <li>{{this.email}}</li>
  </ul>
{{/each}}
 -->
<h1>Constellations</h1>

<div class="usersIndex">
<ol>
    {{#each star}}
    <li>
        <h1>{{Users.Star.name}}</h1>
        <p>{{users.star.description}}, {{user.star.best_viewed}}</p>
    </li>
    {{/each}}
</ol>
<a href="/users/{{users.id}}edit" class="btn btn-default">User Profile</a>
<br>
<a href="/users/star/new" class="btn btn-default">Create a New Star</a>
</div>
